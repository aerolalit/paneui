# robots.txt for paneui.com — open by default.
# The site has nothing private; let every crawler in.
User-agent: *
Allow: /

# Point crawlers at the sitemap so they discover everything in one fetch.
Sitemap: https://paneui.com/sitemap.xml
