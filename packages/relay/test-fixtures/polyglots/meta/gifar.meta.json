{
  "name": "gifar",
  "mime": "image/gif",
  "threatClass": "appended-payload",
  "source": "hand-authored (named after the historical 'GIFAR' GIF/JAR attack documented by Stevens / Mandiant ~2008)",
  "description": "GIF + JAR polyglot. Historical browser-plugin attack: a single file served as image/gif but loadable by Java as a JAR. The corpus retains it as a regression baseline even though Java applets are long-deprecated.",
  "expectedNormalisation": "stripped",
  "assertions": {
    "normalised": true,
    "outputDoesNotContain": ["polyglot test trailer", "Applet.class"]
  }
}
