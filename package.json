{
  "name": "pane",
  "version": "0.0.10",
  "description": "A round-trip UI channel between agents and humans.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @paneui/core && npm run build --workspace @paneui/relay && npm run build --workspace @paneui/cli",
    "typecheck": "npm run build --workspace @paneui/core && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test:unit --workspaces --if-present",
    "test:e2e": "npm run test:e2e --workspace @paneui/relay",
    "test:redis": "npm run test:redis --workspace @paneui/relay",
    "dev": "npm run dev --workspace @paneui/relay",
    "start": "npm run start --workspace @paneui/relay",
    "generate": "npm run generate --workspace @paneui/relay",
    "check:schema-sync": "npm run check:schema-sync --workspace @paneui/relay",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "site:og": "node scripts/render-og.mjs",
    "site:favicons": "node scripts/render-favicons.mjs",
    "prepare": "husky"
  },
  "overrides": {
    "@prisma/instrumentation": {
      "@opentelemetry/instrumentation": "^0.218.0"
    }
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "prettier": "^3.8.3",
    "typescript-eslint": "^8.60.0"
  }
}
